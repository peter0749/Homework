#include <stdio.h>

int main(void){
	
	#if 0
	int x=0, y=0; while( scanf("%d,%d", &x, &y) != EOF){ printf("%d, %d\n", x, y); }
	#endif
	
	#if 1
	int x=0, y=0; while( scanf("%d,%d", &x, &y) != EOF){ printf("%d, %d\n", x, y); }
	#endif
	
	#if 0
	int x=0, y=0; while( scanf("%d,%d", &x, &y) != EOF){ printf("%d, %d\n", x, y); }
	#endif
	system("pause");
	return 0;
}
